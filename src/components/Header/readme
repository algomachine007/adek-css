This component is a higher order component that simply renders the Header elements i.e Dropdown and Hamburger menu items. It is decoupled from the Navigation component which could be easily replaced by any Navigation component of choice.
